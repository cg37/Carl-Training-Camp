#include <iostream>
#include <vector>
#include <string>
#include <vector>
#include <unordered_set>
#include "head.h"

int main() {
    // unordered_map<int,int> map{{0,1},{2,3}};
    // map[0]++;
    // cout<<map[0]<<endl;
    //cout<<map.size();
    string s = " as df ";
    cout<<s.length()<<endl;
    cout<<s.size()<<endl;
    return 0;
}